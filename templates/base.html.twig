<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/base.css') }}">
        {% endblock %}
    </head>

    <body>
        <header>
            <nav class="navbar">
                <div class="logo">
                    <a href="{{ path('app_homepage') }}"> AG </a>
                </div>

                <div class="menu">
                    <a class="nav-link" href="{{ path('app_homepage') }}">Home</a>
                    <a class="nav-link" href="{{ path('app_about') }}">About me</a>
                    <a class="nav-link" href="{{ path('app_work') }}">My work</a>
                    <a class="nav-link" href="{{ path('app_background') }}">My background </a>
                    <a class="nav-link" href="{{ path('app_contact') }}">Get in touch </a>
                </div>
                <div class="link">
                    <a class="nav-link" href="{{ path('app_homepage') }}">LinkedIn </a>
                </div>

            </nav>
        </header>

        <div class="cv">
            <a class="nav-link" href="{{ path('app_homepage') }}">My CV </a>
        </div>

        <div class="page">
            {% block body %}


            {% endblock %}
        </div>

        {% block javascripts %}

        {% endblock %}
    </body>
</html>